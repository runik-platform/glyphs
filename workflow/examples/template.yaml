spellbook:
  name: example
chapter:
  name: workflows

glyphs:
  workflow:
    reconcile-cache:
      type: template
      labels:
        process: cache-reconciliation
      spec:
        entrypoint: reconcile
        synchronization:
          mutexes:
            - name: cache-reconciliation
        templates:
          - name: reconcile
            container:
              image: alpine:3.22
              command: [sh, -c]
              args: ["echo reconcile"]
